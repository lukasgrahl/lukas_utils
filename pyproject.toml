[project]
name = "lukas_utils"
version = "0.1"
description = "utils functions"
readme = "README.md"
authors = [{ name = "Lukas Grahl" }]
requires-python = ">=3.5"
dependencies = ["pandas", "openpyxl"]

